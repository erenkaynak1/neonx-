lign:center; min-height:16px; }
.info-text{ color: var(--text-dim); font-size:13px; text-align:center; margin-top:10px; line-height:1.5;}

.topbar{ display:flex; align-items:center; justify-content:space-between; margin-bottom:14px; }
.back-btn{
  background:none; border:none; color: var(--text-dim); font-size:14px; font-weight:700;
  letter-spacing:1px; cursor:pointer; display:flex; align-items:center; gap:6px; text-transform:uppercase;
}
.back-btn:hover{ color: var(--green); }

.progress-dots{ display:flex; justify-content:center; gap:6px; margin-top:16px; }
.progress-dots span{ width:8px; height:8px; border-radius:50%; background: rgba(255,255,255,0.15); }
.progress-dots span.done{ background: var(--green); box-shadow:0 0 6px rgba(57,255,136,0.8); }
.progress-dots span.current{ background: var(--cyan); box-shadow:0 0 8px rgba(52,230,255,0.9); }

.section-title{
  font-family:'Orbitron',sans-serif; font-size:14px; letter-spacing:2px; text-transform:uppercase;
  color: var(--text-dim); margin-bottom:12px;
}

.hidden{ display:none !important; }
.spinner{
  width:26px; height:26px; border-radius:50%; border:3px solid rgba(57,255,136,0.2);
  border-top-color: var(--green); animation: spin 0.8s linear infinite; margin: 0 auto;
}
@keyframes spin{ to{ transform: rotate(360deg); } }

.screen{
  animation: screenIn 0.38s cubic-bezier(0.22,1,0.36,1);
}
@keyframes screenIn{
  from{ opacity:0; transform: translateY(14px) scale(0.99); }
  to{ opacity:1; transform: translateY(0) scale(1); }
}
.card, .reveal-card{
  animation: cardIn 0.42s cubic-bezier(0.22,1,0.36,1) backwards;
}
.card:nth-of-type(2){ animation-delay: 0.05s; }
.card:nth-of-type(3){ animation-delay: 0.1s; }
@keyframes cardIn{
  from{ opacity:0; transform: translateY(10px); }
  to{ opacity:1; transform: translateY(0); }
}
.title span{ display:inline-block; }

.peel-wrap{
  position:relative;
  width:100%;
  height:232px;
  overflow:hidden;
  perspective:900px;
  border-radius:18px;
  border:2px solid rgba(57,255,136,0.28);
  background:#050b08;
  box-shadow:
    inset 0 0 32px rgba(57,255,136,0.05),
    0 18px 34px rgba(0,0,0,0.26);
  isolation:isolate;
}

/* ALT KAĞIT: her zaman sabit. Gizli rol doğrudan bunun üzerinde. */
.bottom-paper{
  position:absolute;
  inset:10px;
  z-index:1;
  border-radius:15px;
  overflow:hidden;
  display:flex;
  align-items:center;
  justify-content:center;
  flex-direction:column;
  gap:14px;
  padding:22px;
  text-align:center;
  background:
    radial-gradient(circle at 18% 14%, rgba(52,230,255,0.10), transparent 28%),
    linear-gradient(180deg, #07120f 0%, #040a08 100%);
  border:1px solid rgba(52,230,255,0.18);
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.035),
    inset 0 0 28px rgba(52,230,255,0.04);
}
.bottom-paper::before{
  content:"";
  position:absolute;
  inset:0;
  pointer-events:none;
  background:
    repeating-linear-gradient(135deg, rgba(255,255,255,0.018) 0 2px, transparent 2px 12px),
    linear-gradient(90deg, transparent, rgba(57,255,136,0.035), transparent);
}
.bottom-paper .role-label{
  position:relative;
  z-index:1;
  font-size:12px;
  letter-spacing:3px;
  text-transform:uppercase;
  opacity:0.9;
}
.bottom-paper .player-name{
  position:relative;
  z-index:1;
  font-family:'Orbitron',sans-serif;
  font-weight:900;
  font-size:26px;
  line-height:1.2;
  color:#fff;
}
.bottom-paper.normal{
  box-shadow: inset 0 0 34px rgba(57,255,136,0.08);
}
.bottom-paper.normal .role-label{ color:var(--green); }
.bottom-paper.normal .player-name{ text-shadow:0 0 14px rgba(57,255,136,0.70); }
.bottom-paper.imposter{
  box-shadow: inset 0 0 34px rgba(255,51,85,0.10);
}
.bottom-paper.imposter .role-label{ color:var(--red); }
.bottom-paper.imposter .player-name{ text-shadow:0 0 14px rgba(255,51,85,0.76); }

/* ÜST KAĞIDIN ÖN YÜZÜ. Tam kart hareket etmez; sadece clip geometrisi değişir. */
.top-paper-front,
.top-paper-back{
  position:absolute;
  inset:10px;
  border-radius:15px;
  overflow:hidden;
  pointer-events:none;
  will-change:clip-path;
}
.top-paper-front{
  z-index:3;
  background:
    radial-gradient(circle at 20% 14%, rgba(57,255,136,0.15), transparent 25%),
    linear-gradient(180deg, #10231c 0%, #091611 100%);
  border:1px solid rgba(57,255,136,0.25);
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.045),
    inset 0 -14px 20px rgba(0,0,0,0.17),
    0 10px 20px rgba(0,0,0,0.20);
  clip-path:polygon(0 0,100% 0,100% 100%,0 100%);
}
.top-paper-front::before,
.top-paper-back::before{
  content:"";
  position:absolute;
  inset:0;
  pointer-events:none;
  background:
    repeating-linear-gradient(135deg, rgba(255,255,255,0.026) 0 2px, transparent 2px 12px),
    linear-gradient(90deg, transparent, rgba(52,230,255,0.045), transparent);
}
.top-paper-front .paper-body{
  position:absolute;
  inset:0;
  display:flex;
  align-items:center;
  justify-content:center;
  flex-direction:column;
  text-align:center;
  padding:24px;
  color:#e9fff2;
  text-shadow:0 0 9px rgba(57,255,136,0.24);
}
.paper-top-title{
  font-family:'Orbitron',sans-serif;
  font-size:16px;
  font-weight:700;
  letter-spacing:2px;
  text-transform:uppercase;
  margin-top:2px;
}
.paper-top-hint{
  font-size:14px;
  color:#8fd9b1;
  letter-spacing:0.8px;
  margin-top:10px;
}
.paper-icon{
  display:block;
  font-size:34px;
  margin-bottom:10px;
  opacity:0.82;
}

/* Katlanan arka yüz, crease geometrisinin hemen arkasındaki şerittir.
   Bağımsız flap/üçgen değildir; aynı üst kağıdın maskelenmiş arka yüzüdür. */
.top-paper-back{
  z-index:4;
  background:
    radial-gradient(circle at 72% 78%, rgba(255,255,255,0.10), transparent 24%),
    linear-gradient(135deg, rgba(52,230,255,0.09), transparent 34%),
    linear-gradient(180deg, #1a372b 0%, #0c1d16 100%);
  border:0;
  box-shadow:inset 0 1px 0 rgba(255,255,255,0.065);
  clip-path:polygon(100% 100%,100% 100%,100% 100%);
  transform-origin:center;
  will-change:clip-path,filter;
}
.top-paper-back::after{
  content:"";
  position:absolute;
  inset:0;
  pointer-events:none;
  background:linear-gradient(315deg, rgba(0,0,0,0.22), transparent 42%, rgba(255,255,255,0.035));
}

/* Fold çizgisi ve hemen altındaki gölge, drag geometrisine JS ile bağlanır. */
.fold-shadow,
.fold-crease{
  position:absolute;
  left:0;
  top:0;
  z-index:5;
  pointer-events:none;
  transform-origin:0 50%;
  opacity:0;
}
.fold-shadow{
  height:22px;
  background:linear-gradient(180deg, rgba(0,0,0,0.44), rgba(0,0,0,0.12) 38%, rgba(0,0,0,0));
  filter:blur(5px);
}
.fold-crease{
  z-index:6;
  height:1.5px;
  background:linear-gradient(90deg, rgba(52,230,255,0.16), rgba(238,255,246,0.82), rgba(57,255,136,0.50));
  box-shadow:
    0 -2px 5px rgba(255,255,255,0.08),
    0 3px 9px rgba(0,0,0,0.34),
    0 0 9px rgba(57,255,136,0.18);
}

/* Görünmez touch alanı: hiçbir üçgen, handle veya pull-tab çizilmez. */
.peel-hit-zone{
  position:absolute;
  right:10px;
  bottom:10px;
  width:70px;
  height:70px;
  z-index:7;
  background:transparent;
  touch-action:none;
  cursor:grab;
}
.peel-hit-zone:active{ cursor:grabbing; }
.peel-hit-zone.disabled{ pointer-events:none; }

.peel-caption{
  text-align:center;
  font-size:12px;
  letter-spacing:1px;
  color:var(--text-dim);
  margin-top:10px;
}

.rotate-overlay{
  display:none;
  position:fixed; inset:0; z-index:999;
  background: #04070a;
  color: var(--green);
  align-items:center; justify-content:center; flex-direction:column; gap:16px;
  text-align:center; padding:30px;
}
.rotate-overlay .icon{
  font-size:52px; animation: rotateHint 1.6s ease-in-out infinite;
}
@keyframes rotateHint{
  0%,100%{ transform: rotate(0deg); }
  50%{ transform: rotate(-90deg); }
}
.rotate-overlay .msg{ font-family:'Orbitron',sans-serif; font-size:15px; letter-spacing:1px; text-shadow:0 0 10px rgba(57,255,136,0.6); }
@media (orientation: landscape) and (max-height: 600px){
  .rotate-overlay{ display:flex; }
}

.sidegames-return{position:fixed;left:12px;top:12px;z-index:1200;display:flex;align-items:center;gap:7px;padding:9px 12px;border-radius:999px