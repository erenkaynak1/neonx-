import {getApp,getApps} from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-app.js';
import {getAuth,onAuthStateChanged} from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js';
import {getDatabase,get,onValue,ref,remove,runTransaction,serverTimestamp,set,update} from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-database.js';

const DEFAULT_GRACE_MS=30000;
const graceMs=()=>Math.max(250,Number(globalThis.NEON_SOCIAL_OFFLINE_GRACE_MS||DEFAULT_GRACE_MS));
const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));
const state={auth:null,db:null,user:null,profile:null,partyId:'',partyOff:null,partyMissingTimer:null,userOffs:[],memberOffs:new Map(),memberTimers:new Map(),busy:new Set(),reconciling:new Set()};

const partyKey=uid=>`social/userParty/${uid}`;
const friendKey=(a,b)=>`social/friends/${a}/${b}`;
const requestKey=(to,from)=>`social/friendRequests/${to}/${from}`;
const connections=p=>Object.keys(p?.connections||{}).length;

function ensureRuntime(){if(!getApps().length)return false;const app=getApp();state.auth=state.auth||getAuth(app);state.db=state.db||getDatabase(app);state.user=state.user||state.auth.currentUser||null;return Boolean(state.db)}
function me(){ensureRuntime();return state.user||state.auth?.currentUser||null}
function partyRef(id){return ref(state.db,`social/parties/${id}`)}
function announce(text,error=false){const legacy=document.querySelector('.nx-social-status');if(legacy){legacy.textContent=text;legacy.classList.toggle('error',Boolean(error))}const drawer=document.querySelector('.nx-social-drawer-layer.open .nx-drawer-status');if(drawer){drawer.textContent=text;drawer.classList.toggle('error',Boolean(error));drawer.classList.add('show')}}

async function profileFor(uid){ensureRuntime();return (await get(ref(state.db,`social/profiles/${uid}`))).val()||null}
async function currentProfile(){const user=me();if(!user)return null;const live=window.NEON_SOCIAL?.profile;if(live?.username)return live;state.profile=state.profile?.username?state.profile:await profileFor(user.uid);return state.profile}

async function validPartyFor(uid,{waitForReservation=true}={}){ensureRuntime();const pointer=ref(state.db,partyKey(uid)),id=String((await get(pointer)).val()||'');if(!id)return '';const attempts=waitForReservation?8:1;for(let i=0;i<attempts;i++){const party=(await get(partyRef(id))).val();if(party?.members?.[uid])return id;if(i<attempts-1)await sleep(70)}await runTransaction(pointer,current=>String(current||'')===id?null:current,{applyLocally:false});return ''}

async function ensureParty(attempt=0){
  const user=me();if(!user)throw new Error('Aktif oyuncu oturumu yok.');const profile=await currentProfile();if(!profile?.username)throw new Error('Önce oyuncu adını oluştur.');
  const uid=user.uid,existing=await validPartyFor(uid);if(existing)return existing;if(attempt>2)throw new Error('Parti üyeliği aynı anda çok kez değişti. Tekrar dene.');
  const id=`p_${uid.slice(0,8)}_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,7)}`,pointer=ref(state.db,partyKey(uid));
  const claim=await runTransaction(pointer,current=>current||id,{applyLocally:false});if(!claim.committed)throw new Error('Parti üyeliği alınamadı.');const claimed=String(claim.snapshot.val()||'');
  if(claimed!==id){for(let i=0;i<12;i++){const p=(await get(partyRef(claimed))).val();if(p?.members?.[uid])return claimed;await sleep(70)}await runTransaction(pointer,current=>String(current||'')===claimed?null:current,{applyLocally:false});return ensureParty(attempt+1)}
  const created=await runTransaction(partyRef(id),current=>current||({leaderUid:uid,createdAt:Date.now(),members:{[uid]:{username:profile.username,joinedAt:Date.now()}}}),{applyLocally:false});
  if(!created.committed){await runTransaction(pointer,current=>String(current||'')===id?null:current,{applyLocally:false});throw new Error('Parti oluşturulamadı.')}
  const stabilized=await runTransaction(pointer,current=>!current||current===id?id:current,{applyLocally:false}),finalId=String(stabilized.snapshot.val()||'');
  if(finalId!==id){await runTransaction(partyRef(id),current=>current&&Object.keys(current.members||{}).length===1&&current.members?.[uid]?null:current,{applyLocally:false});const valid=await validPartyFor(uid);if(valid)return valid;return ensureParty(attempt+1)}
  announce('Kalıcı parti kuruldu.');return id;
}

async function acceptFriend(uid){const user=me();if(!user||!uid)throw new Error('Arkadaşlık isteği geçersiz.');const profile=await currentProfile(),incoming=(await get(ref(state.db,requestKey(user.uid,uid)))).val(),other=await profileFor(uid),already=(await get(ref(state.db,friendKey(user.uid,uid)))).exists();if(!incoming&&!already)throw new Error('Arkadaşlık isteği artık mevcut değil.');const otherName=incoming?.username||other?.username||'Oyuncu';await update(ref(state.db),{[friendKey(user.uid,uid)]:{username:otherName,since:serverTimestamp()},[friendKey(uid,user.uid)]:{username:profile?.username||'Oyuncu',since:serverTimestamp()},[requestKey(user.uid,uid)]:null,[requestKey(uid,user.uid)]:null});announce(`@${otherName} arkadaşlara eklendi.`);return true}
async function sendFriend(uid,username=''){const user=me();if(!user||!uid||uid===user.uid)throw new Error('Bu oyuncuya istek gönderilemez.');const profile=await currentProfile();if(!profile?.username)throw new Error('Önce oyuncu adını oluştur.');if((await get(ref(state.db,friendKey(user.uid,uid)))).exists()){announce(`@${username||'Oyuncu'} zaten arkadaşın.`);return {already:true}}const reverse=(await get(ref(state.db,requestKey(user.uid,uid)))).val();if(reverse){await acceptFriend(uid);return {accepted:true}}await runTransaction(ref(state.db,requestKey(uid,user.uid)),current=>current||({username:profile.username,createdAt:Date.now()}),{applyLocally:false});const crossed=(await get(ref(state.db,requestKey(user.uid,uid)))).val();if(crossed){await acceptFriend(uid);return {accepted:true}}announce(`@${username||'Oyuncu'} için arkadaşlık isteği gönderildi.`);return {sent:true}}
async function removeFriend(uid){const user=me();if(!user||!uid)return false;const other=(await get(ref(state.db,friendKey(user.uid,uid)))).val();await update(ref(state.db),{[friendKey(user.uid,uid)]:null,[friendKey(uid,user.uid)]:null,[requestKey(user.uid,uid)]:null,[requestKey(uid,user.uid)]:null});announce(`@${other?.username||'Oyuncu'} arkadaş listesinden çıkarıldı.`);return true}
async function reconcileRequest(fromUid,request){const user=me();if(!user||!fromUid||state.reconciling.has(fromUid))return;state.reconciling.add(fromUid);try{const already=(await get(ref(state.db,friendKey(user.uid,fromUid)))).exists();if(already){await update(ref(state.db),{[requestKey(user.uid,fromUid)]:null,[requestKey(fromUid,user.uid)]:null});return}const reverse=(await get(ref(state.db,requestKey(fromUid,user.uid)))).val();if(!reverse)return;const profile=await currentProfile(),other=await profileFor(fromUid),otherName=request?.username||other?.username||'Oyuncu';await update(ref(state.db),{[friendKey(user.uid,fromUid)]:{username:otherName,since:serverTimestamp()},[friendKey(fromUid,user.uid)]:{username:profile?.username||'Oyuncu',since:serverTimestamp()},[requestKey(user.uid,fromUid)]:null,[requestKey(fromUid,user.uid)]:null})}finally{state.reconciling.delete(fromUid)}}

async function inviteFriend(uid){const user=me();if(!user||!uid)throw new Error('Oyuncu bulunamadı.');const profile=await currentProfile();if(!(await get(ref(state.db,friendKey(user.uid,uid)))).exists())throw new Error('Parti daveti yalnızca arkadaşlara gönderilebilir.');const id=await ensureParty(),targetParty=await validPartyFor(uid);if(targetParty===id){await remove(ref(state.db,`social/partyInvites/${uid}/${id}`));announce('Bu oyuncu zaten senin partinde.');return {already:true}}if(targetParty)throw new Error('Bu oyuncu zaten başka bir aktif partide.');await set(ref(state.db,`social/partyInvites/${uid}/${id}`),{fromUid:user.uid,fromName:profile?.username||'Oyuncu',createdAt:serverTimestamp()});announce('Parti daveti gönderildi.');return {sent:true,partyId:id}}

async function acceptParty(id){
  const user=me();if(!user||!id)throw new Error('Parti daveti geçersiz.');const uid=user.uid,profile=await currentProfile(),existing=await validPartyFor(uid);
  if(existing===id){await remove(ref(state.db,`social/partyInvites/${uid}/${id}`));announce('Zaten bu partidesin.');return {already:true}}if(existing)throw new Error('Zaten aktif bir partidesin. Önce mevcut partiden ayrıl.');
  const target=partyRef(id),serverParty=(await get(target)).val();
  if(!serverParty){await remove(ref(state.db,`social/partyInvites/${uid}/${id}`));throw new Error('Parti artık mevcut değil. Davet temizlendi.')}
  const pointer=ref(state.db,partyKey(uid)),claim=await runTransaction(pointer,current=>!current||current===id?id:undefined,{applyLocally:false});if(!claim.committed)throw new Error('Başka bir parti üyeliği aynı anda etkinleşti.');
  const joined=await runTransaction(target,current=>{if(!current)return;const members={...(current.members||{})};members[uid]=members[uid]||{username:profile?.username||'Oyuncu',joinedAt:Date.now()};const next={...current,members};if(!next.leaderUid||!members[next.leaderUid])next.leaderUid=Object.keys(members)[0];return next},{applyLocally:false});
  if(!joined.committed){await runTransaction(pointer,current=>String(current||'')===id?null:current,{applyLocally:false});await remove(ref(state.db,`social/partyInvites/${uid}/${id}`));throw new Error('Parti artık mevcut değil. Davet temizlendi.')}
  await remove(ref(state.db,`social/partyInvites/${uid}/${id}`));announce('Partiye katıldın.');return {joined:true,partyId:id};
}

async function cleanupMember(id,uid,force=false){if(!id||!uid)return false;ensureRuntime();if(!force){const p=(await get(ref(state.db,`social/presence/${uid}`))).val()||{};if(connections(p)>0)return false;const last=Number(p.lastSeen||0);if(last&&Date.now()-last<graceMs())return false}const tx=await runTransaction(partyRef(id),current=>{if(!current?.members?.[uid])return current;const members={...(current.members||{})};delete members[uid];const remaining=Object.keys(members);if(!remaining.length)return null;const next={...current,members};if(next.leaderUid===uid||!members[next.leaderUid])next.leaderUid=remaining[0];return next},{applyLocally:false});await runTransaction(ref(state.db,partyKey(uid)),current=>String(current||'')===id?null:current,{applyLocally:false});await remove(ref(state.db,`social/partyInvites/${uid}/${id}`)).catch(()=>{});return Boolean(tx.committed)}
async function leaveOwnParty(){const user=me();if(!user)return false;if(window.NEON_PARTY_MEMBERSHIP?.leaveCurrentParty){const result=await window.NEON_PARTY_MEMBERSHIP.leaveCurrentParty();return Boolean(result?.left)}const id=await validPartyFor(user.uid);if(!id)return false;return cleanupMember(id,user.uid,true)}

function clearMemberWatchers(){for(const off of state.memberOffs.values())try{off()}catch{}state.memberOffs.clear();for(const timer of state.memberTimers.values())clearTimeout(timer);state.memberTimers.clear()}
function scheduleOfflineCleanup(id,uid,presence){const old=state.memberTimers.get(uid);if(old)clearTimeout(old);if(connections(presence)>0){state.memberTimers.delete(uid);return}const last=Number(presence?.lastSeen||Date.now()),delay=Math.max(0,graceMs()-(Date.now()-last));state.memberTimers.set(uid,setTimeout(async()=>{state.memberTimers.delete(uid);try{await cleanupMember(id,uid,false)}catch(error){console.warn('[NEON XI] offline party cleanup failed',error)}},delay+50))}
async function reconcileMissingParty(id){const user=me();if(!user||!id)return;const pointer=ref(state.db,partyKey(user.uid)),currentId=String((await get(pointer)).val()||'');if(currentId!==id)return;const fresh=(await get(partyRef(id))).val();if(fresh?.members?.[user.uid])return;await runTransaction(pointer,current=>String(current||'')===id?null:current,{applyLocally:false})}
function watchParty(id){clearMemberWatchers();state.partyOff?.();state.partyOff=null;if(state.partyMissingTimer){clearTimeout(state.partyMissingTimer);state.partyMissingTimer=null}if(!id)return;state.partyOff=onValue(partyRef(id),snap=>{const party=snap.val();if(!party?.members){clearMemberWatchers();if(state.partyMissingTimer)clearTimeout(state.partyMissingTimer);state.partyMissingTimer=setTimeout(()=>{state.partyMissingTimer=null;reconcileMissingParty(id).catch(error=>console.warn('[NEON XI] stale party pointer cleanup failed',error))},1000);return}if(state.partyMissingTimer){clearTimeout(state.partyMissingTimer);state.partyMissingTimer=null}const active=new Set(Object.keys(party.members).filter(uid=>uid!==me()?.uid));for(const [uid,off] of state.memberOffs){if(active.has(uid))continue;try{off()}catch{}state.memberOffs.delete(uid);const timer=state.memberTimers.get(uid);if(timer)clearTimeout(timer);state.memberTimers.delete(uid)}for(const uid of active){if(state.memberOffs.has(uid))continue;state.memberOffs.set(uid,onValue(ref(state.db,`social/presence/${uid}`),s=>scheduleOfflineCleanup(id,uid,s.val()||{})))}})}

function clearUserBindings(){state.userOffs.splice(0).forEach(off=>{try{off()}catch{}});state.partyOff?.();state.partyOff=null;if(state.partyMissingTimer){clearTimeout(state.partyMissingTimer);state.partyMissingTimer=null}clearMemberWatchers();state.partyId='';state.profile=null;state.reconciling.clear()}
function bindUser(user){clearUserBindings();state.user=user;state.userOffs.push(onValue(ref(state.db,`social/profiles/${user.uid}`),s=>{state.profile=s.val()||null}));state.userOffs.push(onValue(ref(state.db,partyKey(user.uid)),s=>{state.partyId=String(s.val()||'');watchParty(state.partyId)}));state.userOffs.push(onValue(ref(state.db,`social/friendRequests/${user.uid}`),s=>{for(const [fromUid,request] of Object.entries(s.val()||{}))reconcileRequest(fromUid,request).catch(error=>console.warn('[NEON XI] friend request reconcile failed',error))}))}

async function handleGuardedAction(button){const user=me();if(!user)return false;if(button.matches('[data-act="create-party"]')){await ensureParty();return true}if(button.matches('[data-invite],[data-nx-party-invite]')){await inviteFriend(button.dataset.invite||button.dataset.nxPartyInvite);return true}if(button.matches('[data-party-accept],[data-nx-party-accept]')){await acceptParty(button.dataset.partyAccept||button.dataset.nxPartyAccept);return true}if(button.matches('[data-nx-confirm-remove]')){const uid=button.closest('[data-nx-friend-menu]')?.dataset?.nxFriendMenu;if(uid)await removeFriend(uid);return Boolean(uid)}if(button.matches('[data-act="logout"]')){await leaveOwnParty().catch(()=>{});await window.NEON_SOCIAL?.signOut?.();return true}return false}
function installActionGuard(){document.addEventListener('click',event=>{const button=event.target.closest?.('button');if(!button||!ensureRuntime()||!me())return;const guarded=button.matches('[data-act="create-party"],[data-invite],[data-nx-party-invite],[data-party-accept],[data-nx-party-accept],[data-nx-confirm-remove],[data-act="logout"]');if(!guarded)return;event.preventDefault();event.stopImmediatePropagation();const lock=`${button.dataset.act||button.dataset.invite||button.dataset.nxPartyInvite||button.dataset.partyAccept||button.dataset.nxPartyAccept||button.outerHTML.slice(0,60)}`;if(state.busy.has(lock))return;state.busy.add(lock);button.disabled=true;handleGuardedAction(button).catch(error=>{console.error('[NEON XI] social consistency action failed',error);announce(error?.message||'Sosyal işlem tamamlanamadı.',true)}).finally(()=>{state.busy.delete(lock);if(button.isConnected)button.disabled=false})},true)}

async function diagnostics(){const user=me();if(!user)return {uid:'',partyId:'',friends:{},requests:{},invites:{},party:null};const partyId=await validPartyFor(user.uid),[friends,requests,invites,party]=await Promise.all([get(ref(state.db,`social/friends/${user.uid}`)),get(ref(state.db,`social/friendRequests/${user.uid}`)),get(ref(state.db,`social/partyInvites/${user.uid}`)),partyId?get(partyRef(partyId)):Promise.resolve({val:()=>null})]);return {uid:user.uid,partyId,friends:friends.val()||{},requests:requests.val()||{},invites:invites.val()||{},party:party.val?.()||null}}
function boot(){installActionGuard();const start=()=>{if(!ensureRuntime())return false;onAuthStateChanged(state.auth,user=>{if(user)bindUser(user);else{clearUserBindings();state.user=null}});return true};if(start())return;let tries=0;const timer=setInterval(()=>{tries++;if(start()||tries>160)clearInterval(timer)},100)}

window.NEON_SOCIAL_CONSISTENCY={ensureParty,sendFriend,acceptFriend,removeFriend,inviteFriend,acceptParty,leaveOwnParty,cleanupMember,diagnostics,get graceMs(){return graceMs()}};
boot();
