--- index(20260816-225154)(1).html
+++ index.html
@@ -1,7 +1,8 @@
 <!-- NEON XI PLAYER POOL: +236 verified FMInside first-team players · 2026-08-16 -->
 <!DOCTYPE html>
 <!-- NEON XI UPDATE: FULL-HISTORY + TACTICAL-SPREAD + INLINE-EVENTS 2026-08-16 -->
+<!-- NEON XI PATCH: ENGINE-EVENT-SYNC + 100X SIM LAB 2026-08-17 -->
 
 <html lang="tr">
 <head>
 <!-- NEON XI CLUB DATA: Transfermarkt · 2026-08-16 · active clubs + former clubs -->
@@ -7522,8 +7523,21 @@
     if(cfg.general==="Savunma Ağırlıklı")
       return "Savunma Ağırlıklı plan takım boyunu kısalttı ve topun gerisindeki güvenliği artırdı.";
     return "Formasyon referans bölgeleri, oyuncu görevleri ve topun bulunduğu koridor birlikte bu yerleşimi oluşturdu.";
   }
+  function visualStatsSnapshot(){
+    const pick=team=>{
+      const s=game.stats?.[team]||{};
+      return {
+        attacks:Number(s.attacks||0),dangerousAttacks:Number(s.dangerousAttacks||0),boxEntries:Number(s.boxEntries||0),
+        transitionAttacks:Number(s.transitionAttacks||0),shots:Number(s.shots||0),onTarget:Number(s.onTarget||0),
+        xG:Number(s.xG||0),passesAttempted:Number(s.passesAttempted||0),passesCompleted:Number(s.passesCompleted||0),
+        turnovers:Number(s.turnovers||0),highTurnovers:Number(s.highTurnovers||0),corners:Number(s.corners||0),
+        saves:Number(s.saves||0),fouls:Number(s.fouls||0),yellowCards:Number(s.yellowCards||0),redCards:Number(s.redCards||0)
+      };
+    };
+    return{A:pick("A"),B:pick("B")};
+  }
   function emitVisualFrame(update={}) {
     const previous=game.visualContext||{};
     const team=update.team||previous.team||((game.momentum||0)>=0?"A":"B");
     const route=update.route||previous.route||"center";
@@ -7541,13 +7555,18 @@
       carrierId:update.carrierId||null,
       defenderId:update.defenderId||null,
       supportId:update.supportId||null,
       ballTargetId:update.ballTargetId||null,
+      turnoverReason:update.turnoverReason||"",
       outcome:update.outcome??"",
       shotId:update.shotId??null,
       message:update.message||previous.message||"Maç yerleşimi",
       cause:update.cause||visualPublicCause(team,route,stage,action),
       eventKind:update.eventKind||"",
+      score:{A:Number(game.score?.A||0),B:Number(game.score?.B||0)},
+      statsSnapshot:visualStatsSnapshot(),
+      possession:{A:Number(game.possession?.A||50),B:Number(game.possession?.B||50)},
+      statDelta:update.statDelta||null,
       timestamp:Date.now()
     };
     game.visualContext=frame;
     game.visualTimeline.push(frame);
@@ -7590,9 +7609,9 @@
         cause:visualPublicCause(shotEvent?.team||game.visualContext?.team||"A",shotEvent?.route||game.visualContext?.route||"center",stage,shotEvent?"shot":game.visualContext?.action||"")
       });
     const event = { minute:game.minute, kind, text, detail, cause, visual };
     game.eventLog.push(event);
-    if (["goal","penalty","yellow","red"].includes(kind)) {
+    if (!game.testMode && ["goal","penalty","yellow","red"].includes(kind)) {
       try {
         window.dispatchEvent(new CustomEvent("neon-xi:match-event", {
           detail: {
             eventId: visual?.id || `${game.token || 0}-${game.minute}-${kind}-${game.eventLog.length}`,
@@ -8102,19 +8121,20 @@
     });
     const carrierSkill=v3ClampRating(stat(p.carrier,"dribbling"));
     const centerCarryChance=clamp(.18+(carrierSkill-50)*.006,.18,.56);
     const shouldCarry=phase>=2&&(route==="wing"||phase===4||rng()<centerCarryChance);
+    let carryExpected=null,carrySuccess=null;
     if(shouldCarry){
-      const carryExpected=clamp(probability*.64+carrierSkill*.0032+.02,.18,.90);
-      const carrySuccess=success?rng()<clamp(.55+carrierSkill*.0032,.58,.86):rng()<clamp(.10+carrierSkill*.0018,.14,.34);
+      carryExpected=clamp(probability*.64+carrierSkill*.0032+.02,.18,.90);
+      carrySuccess=success?rng()<clamp(.55+carrierSkill*.0032,.58,.86):rng()<clamp(.10+carrierSkill*.0018,.14,.34);
       applyMicroRating(attackingTeam,p.carrier,"carrying",carrySuccess,carryExpected,phase===3?.074:phase===4?.086:.052,{
         dribblesAttempted:1,dribblesCompleted:carrySuccess?1:0,carries:carrySuccess?1:0,turnovers:carrySuccess?0:1
       });
       setVisualPhase("dribble",{
         team:attackingTeam,route,phase,action:"dribble",success:carrySuccess,
         focusIds:visualMemberIds(p.carrier,p.defender,p.support),
         carrierId:p.carrier?.footballer?.id||null,defenderId:p.defender?.footballer?.id||null,supportId:p.support?.footballer?.id||null,
-        ballTargetId:p.carrier?.footballer?.id||null,
+        ballTargetId:carrySuccess?p.carrier?.footballer?.id||null:p.defender?.footballer?.id||null,
         message:carrySuccess
           ?`${p.carrier?.footballer?.name||game.teamNames[attackingTeam]} topu sürerek rakibini geçti`
           :`${p.defender?.footballer?.name||game.teamNames[defendingTeam]} bire birde top sürmeyi durdurdu`,
         cause:carrySuccess
@@ -8127,17 +8147,18 @@
     });
     applyMicroRating(attackingTeam,p.support,"positioning",success,probability,.018,{
       positionalActions:1,positionalWins:success?1:0,positionalErrors:success?0:1
     });
+    return{...p,shouldCarry,carryExpected,carrySuccess,carrierSkill};
   }
   function v3RunPhase(attackingTeam,defendingTeam,phase,route) {
     const scores=v3PhaseScores(attackingTeam,defendingTeam,phase,route);
     const attackAdjustment=v3FinalAdjustment(attackingTeam,"attack",phase,route);
     const defenseAdjustment=v3FinalAdjustment(defendingTeam,"defense",phase,route);
     const probability=v3PhaseProbability(scores.attack,scores.defense,attackAdjustment,defenseAdjustment,phase);
     const success=rng()<probability;
-    v3RecordPhaseMicro(attackingTeam,defendingTeam,phase,route,success,probability);
-    return{success,probability,scores,attackAdjustment,defenseAdjustment};
+    const micro=v3RecordPhaseMicro(attackingTeam,defendingTeam,phase,route,success,probability);
+    return{success,probability,scores,attackAdjustment,defenseAdjustment,micro};
   }
   function v3DirectionRoute(teamOrDirection,directionValue) {
     const hasTeam=directionValue!==undefined;
     const team=hasTeam?teamOrDirection:null;
@@ -8305,10 +8326,13 @@
     if(rng()<.80){
       game.stats[attackingTeam].turnovers++;
       addMicroBonus(attackingTeam,result.creator,"passing",-.055,"dangerousTurnovers");
       recordRoleEvent(attackingTeam,result.creator,"turnovers");
-      setVisualPhase("turnover",{team:defendingTeam,route,phase:5,success:false,action:"interception",focusIds:visualMemberIds(result.creator,result.receiver),message:`${game.teamNames[defendingTeam]} giriş pasını kesti`});
-      addEvent("turnover",`${game.teamNames[attackingTeam]} ceza sahasına giriş pasını kaybetti.`,`${Math.round(result.probability*100)}% başarı olasılığı`,"Pas kanalı savunması giriş kombinasyonunu bozdu.");
+      const entryDefender=result.defenders?.[0]?.member||null;
+      const entryDefenderName=entryDefender?.footballer?.name||game.teamNames[defendingTeam];
+      const entryText=`${result.creator?.footballer?.name||game.teamNames[attackingTeam]} ceza sahası giriş pasını denedi; ${entryDefenderName} araya girdi.`;
+      setVisualPhase("turnover",{team:defendingTeam,route,phase:5,success:false,action:"interception",turnoverReason:"entryInterception",focusIds:visualMemberIds(result.creator,result.receiver,entryDefender),carrierId:result.creator?.footballer?.id||null,defenderId:entryDefender?.footballer?.id||null,supportId:result.receiver?.footballer?.id||null,ballTargetId:entryDefender?.footballer?.id||null,message:entryText,cause:`${entryDefenderName} pas kanalını okuyarak ceza sahası giriş kombinasyonunu bozdu.`,statDelta:{team:attackingTeam,turnovers:1}});
+      addEvent("turnover",entryText,`${Math.round(result.probability*100)}% başarı olasılığı`,`${entryDefenderName} pas kanalını okuyarak giriş kombinasyonunu bozdu.`);
       if(rng()<.20)game.forcedAttack={team:defendingTeam,route:"transition",bonus:1};return"end";
     }
     setVisualPhase("entry",{team:attackingTeam,route,phase:5,success:false,action:"secondBall",focusIds:visualMemberIds(result.creator,result.receiver),message:`${game.teamNames[attackingTeam]} ikinci topu aldı`});
     addEvent("tactical",`${game.teamNames[attackingTeam]} ikinci topu aldı.`,"Aynı hücumda pasla giriş tekrar denenmeyecek.","Başarısız giriş pasının %20 ikinci top sonucu.");return"secondBall";
@@ -8326,13 +8350,24 @@
     const defenderAerial=v3ApplyAdjustment((v3ClampRating(stat(defender,"heading"))*.45+v3ClampRating(stat(defender,"defending"))*.30+v3ClampRating(stat(defender,"strength"))*.20+v3ClampRating(stat(defender,"positioning"))*.05)*roleExecution(defender),defenseAdj);
     const duel=clamp(.50+(attackerAerial-defenderAerial)*.008,.15,.85);
     const targetableSuccess=rng()<=targetable;
     applyMicroRating(attackingTeam,creator,"passing",targetableSuccess,targetable,.085,{passesAttempted:1,passesCompleted:targetableSuccess?1:0,progressivePasses:targetableSuccess?1:0,turnovers:targetableSuccess?0:1});
-    if(!targetableSuccess){addEvent("tactical",`${creator.footballer.name} ortasında hedefi bulamadı.`,`${Math.round(targetable*100)}% hedeflenebilir orta`,"Orta savunma tarafından etkisizleştirildi.");return;}
+    if(!targetableSuccess){
+      const deliveryLost=crossDelivery+4<defenderAerial;
+      const crossText=deliveryLost
+        ?`${creator.footballer.name} ortayı istediği noktaya gönderemedi; top savunmaya geçti.`
+        :`${defender?.footballer?.name||game.teamNames[defendingTeam]} ${creator.footballer.name} tarafından yapılan ortanın önüne geçti.`;
+      setVisualPhase("turnover",{team:defendingTeam,route:"wing",phase:5,success:false,action:"interception",turnoverReason:deliveryLost?"poorCross":"crossInterception",focusIds:visualMemberIds(creator,target,defender),carrierId:creator?.footballer?.id||null,defenderId:defender?.footballer?.id||null,supportId:target?.footballer?.id||null,ballTargetId:defender?.footballer?.id||null,message:crossText,cause:deliveryLost?`${creator.footballer.name} orta kalitesi hedef koşusuyla buluşmadı.`:`${defender?.footballer?.name||"Savunmacı"} ortanın geliş yönünü okuyarak hedef oyuncunun önüne geçti.`});
+      addEvent("turnover",crossText,`${Math.round(targetable*100)}% hedeflenebilir orta`,deliveryLost?"Orta hedef koşusuyla buluşmadı.":"Savunmacı orta kanalını kapattı.");return;
+    }
     const duelSuccess=rng()<=duel;
     applyMicroRating(attackingTeam,target,"duels",duelSuccess,duel,.065,{duelsAttempted:1,duelsWon:duelSuccess?1:0});
     applyMicroRating(defendingTeam,defender,"duels",!duelSuccess,1-duel,.070,{duelsAttempted:1,duelsWon:!duelSuccess?1:0,defensiveActions:1});
-    if(!duelSuccess){addEvent("tactical",`${creator.footballer.name} ortasında savunma hava topunu kazandı.`,`${Math.round(duel*100)}% hücum hava düellosu` ,"Hava savunması üstün geldi.");if(rng()<.20)return v3ResolveGroundShot(attackingTeam,defendingTe